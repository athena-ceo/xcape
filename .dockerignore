node_modules
dist
dist-ssr
.git
.venv
venv
xcvenv
__pycache__
*.pyc
.pytest_cache
.mypy_cache
.ruff_cache
logs
*.sqlite
*.db
.env
.env.*
.DS_Store
# Runtime data created by the prod stack (postgres volume, backups). These live under
# the repo on the server and are owned by the container — must never enter a build
# context (unreadable + would bloat/leak the image).
data/
backups/
backend/xcvenv
backend/gpvenv
